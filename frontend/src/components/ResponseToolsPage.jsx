import { useState } from "react";
import Header from "./Header";
import { ROUTES } from "../config/routes";
import "./ResponseToolsPage.css";
const cases = [{ title: "공급망 이슈 확산", result: "선제 공지와 FAQ 배포 후 부정 언급이 31% 감소했습니다." }, { title: "제품 품질 논란", result: "고객 안내와 점검 계획 공개가 신뢰 회복에 효과적이었습니다." }, { title: "규제 리스크 발생", result: "전담 TF 구성과 이해관계자 브리핑이 확산을 완화했습니다." }];
const documents = ["보도자료", "고객 안내문", "임직원 공지", "Q&A 문서"];
function ResponseToolsPage({ mode }) { const [activeMode, setActiveMode] = useState(mode); const [selectedCase, setSelectedCase] = useState(0); const [document, setDocument] = useState(documents[0]); const [generated, setGenerated] = useState(false); const simulator = activeMode === "simulator"; const changeMode = (next) => { setActiveMode(next); window.history.pushState({}, "", next === "simulator" ? ROUTES.CASE_SIMULATOR : ROUTES.RESPONSE_GENERATOR); }; return <div className="response-page"><Header /><main className="response-main"><p className="response-kicker">AI RESPONSE CENTER</p><h1>{simulator ? "과거 사례 시뮬레이터" : "대응자료 생성"}</h1><p className="response-description">과거 대응 사례를 참고하고, 현재 이슈에 맞는 자료 초안을 빠르게 준비하세요.</p><div className="response-tabs"><button type="button" className={simulator ? "active" : ""} onClick={() => changeMode("simulator")}>과거 사례 시뮬레이터</button><button type="button" className={!simulator ? "active" : ""} onClick={() => changeMode("generator")}>대응자료 생성</button></div>{simulator ? <section className="tool-grid"><div className="tool-card"><span>CASE LIBRARY</span><h2>유사 사례를 선택하세요</h2><div className="case-options">{cases.map((item, index) => <button type="button" className={selectedCase === index ? "selected" : ""} onClick={() => setSelectedCase(index)} key={item.title}>{item.title}<i>›</i></button>)}</div></div><div className="tool-card simulation-result"><span>SIMULATION RESULT</span><h2>{cases[selectedCase].title}</h2><p>{cases[selectedCase].result}</p><div className="result-metrics"><p><b>31%</b><span>부정 언급 감소</span></p><p><b>2.4일</b><span>정상화 예상</span></p></div><button type="button" className="primary-action">이 대응안 적용하기</button></div></section> : <section className="tool-card generator-card"><span>AI DOCUMENT DRAFT</span><h2>대응 자료 초안을 생성하세요</h2><label>문서 유형<select value={document} onChange={(event) => setDocument(event.target.value)}>{documents.map((item) => <option key={item}>{item}</option>)}</select></label><label>핵심 상황<textarea defaultValue="공급망 이슈와 관련한 고객 문의가 증가하고 있습니다." /></label><button type="button" className="primary-action" onClick={() => setGenerated(true)}>AI 초안 생성하기</button>{generated && <div className="generated-draft"><b>{document} 초안이 준비되었습니다.</b><p>현재 상황을 인지하고 있으며, 고객과 이해관계자에게 정확한 정보를 신속하게 안내하겠습니다.</p></div>}</section>}</main></div>; }
export default ResponseToolsPage;