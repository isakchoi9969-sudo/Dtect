{
  "name": "dtect",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently --names frontend,backend --prefix-colors blue,green \"npm --prefix frontend run dev\" \"npm --prefix backend/server run dev\"",
    "dev:ai": "cd backend\\ai-server && .venv\\Scripts\\python.exe run.py",
    "dev:all": "concurrently --names frontend,backend,ai --prefix-colors blue,green,magenta \"npm --prefix frontend run dev\" \"npm --prefix backend/server run dev\" \"npm run dev:ai\"",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
